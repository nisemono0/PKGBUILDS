diff --git a/config.def.h b/config.def.h
index 3c7d9d4..57e37b8 100644
--- a/config.def.h
+++ b/config.def.h
@@ -26,3 +26,6 @@ static const char worddelimiters[] = " ";
 
 /* Size of the window border */
 static unsigned int border_width = 0;
+
+/* use locale modifier */
+static unsigned int locale_mod = 0;
diff --git a/dmenu.1 b/dmenu.1
index 2d7784b..6c94213 100644
--- a/dmenu.1
+++ b/dmenu.1
@@ -52,6 +52,9 @@ is faster, but will lock up X until stdin reaches end\-of\-file.
 .B \-i
 dmenu matches menu items case insensitively.
 .TP
+.B \-lm
+dmenu tries to set locale modifiers. (this messes with window focus)
+.TP
 .BI \-bw " border_size"
 set border size of the dmenu window
 .TP
diff --git a/dmenu.c b/dmenu.c
index 7aa5e91..106f450 100644
--- a/dmenu.c
+++ b/dmenu.c
@@ -731,7 +731,7 @@ run(void)
 	XEvent ev;
 
 	while (!XNextEvent(dpy, &ev)) {
-		if (XFilterEvent(&ev, win))
+		if (XFilterEvent(&ev, None))
 			continue;
 		switch(ev.type) {
 		case DestroyNotify:
@@ -877,6 +877,9 @@ setup(void)
 	                XNClientWindow, win, XNFocusWindow, win, NULL);
 
 	XMapRaised(dpy, win);
+    if(locale_mod) {
+        grabfocus();
+    }
 	if (embed) {
 		XSelectInput(dpy, parentwin, FocusChangeMask | SubstructureNotifyMask);
 		if (XQueryTree(dpy, parentwin, &dw, &w, &dws, &du) && dws) {
@@ -917,7 +920,9 @@ main(int argc, char *argv[])
 		else if (!strcmp(argv[i], "-i")) { /* case-insensitive item matching */
 			fstrncmp = strncasecmp;
 			fstrstr = cistrstr;
-		} else if (i + 1 == argc)
+		} else if (!strcmp(argv[i], "-lm"))/* use locale modifiers */
+            locale_mod = 1;
+        else if (i + 1 == argc)
 			usage();
 		/* these options take one argument */
 		else if (!strcmp(argv[i], "-l"))   /* number of lines in vertical list */
@@ -948,6 +953,11 @@ main(int argc, char *argv[])
 
 	if (!setlocale(LC_CTYPE, "") || !XSupportsLocale())
 		fputs("warning: no locale support\n", stderr);
+    if(locale_mod) {
+        if(!XSetLocaleModifiers("")){
+            fputs("warning: no locale modifiers support\n", stderr);
+        }
+    }
 	if (!(dpy = XOpenDisplay(NULL)))
 		die("cannot open display");
 	screen = DefaultScreen(dpy);
